- name: Run Python script with survey input
  hosts: localhost
  gather_facts: false
  vars:
    root_dir: "{{ survey_root_dir }}"
  tasks:
    - name: Run the Python script with root_dir as argument
      script: test.py
      args:
        argv:
          - "{{ root_dir }}"
